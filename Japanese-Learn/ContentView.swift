import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 16) {

            Text("Japanese Learn")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}

